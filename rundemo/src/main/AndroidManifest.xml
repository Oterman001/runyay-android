<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 网络权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>

    <!-- 前台服务权限（数据同步） -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- APK 安装权限 -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- 应用市场包名查询（Android 11+ Package Visibility） -->
    <queries>
        <package android:name="com.heytap.market" />
        <package android:name="com.huawei.appmarket" />
        <package android:name="com.xiaomi.market" />
        <package android:name="com.bbk.appstore" />
        <package android:name="com.android.vending" />
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@drawable/run_demo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/run_demo"
        android:supportsRtl="true"
        android:name=".MyRunApplication"
        android:theme="@style/Theme.ComopseDemoHub"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:label="@string/app_name"
            android:theme="@style/Theme.ComopseDemoHub">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- 友盟深链接 -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="um.69a930fe6f259537c76ddab0" />
            </intent-filter>

            <!-- 接收"打开方式"：.fit 文件（application/octet-stream） -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/octet-stream" />
            </intent-filter>

            <!-- 接收"打开方式"：.fit 文件，MIME type 为 null（微信等 App 不识别扩展名时） -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="content" android:pathPattern=".*\\.fit" />
                <data android:scheme="file" android:pathPattern=".*\\.fit" />
            </intent-filter>

            <!-- 接收"打开方式"：.fit 文件，MIME type 为任意非 null 值（含 */*） -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="content" android:mimeType="*/*" android:pathPattern=".*\\.fit" />
                <data android:scheme="file" android:mimeType="*/*" android:pathPattern=".*\\.fit" />
            </intent-filter>

            <!-- 接收"打开方式"：.zip 压缩包 -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/zip" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/x-zip-compressed" />
            </intent-filter>

            <!-- 接收单文件分享（ACTION_SEND） -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/octet-stream" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/zip" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/x-zip-compressed" />
            </intent-filter>

            <!-- 接收多文件分享（ACTION_SEND_MULTIPLE） -->
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/octet-stream" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/zip" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/x-zip-compressed" />
            </intent-filter>

        </activity>

        <activity
            android:name=".RunDetailActivity"
            android:exported="false"
            android:label="跑步详情"
            android:theme="@style/Theme.ComopseDemoHub"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".RunningShoeDetailActivity"
            android:exported="false"
            android:label="跑鞋详情"
            android:theme="@style/Theme.ComopseDemoHub"
            android:parentActivityName=".RunDetailActivity" />

        <activity
            android:name=".presentation.feature.share.ShareActivity"
            android:exported="false"
            android:label="分享跑步"
            android:theme="@style/Theme.ComopseDemoHub"
            android:parentActivityName=".RunDetailActivity" />

        <!-- FileProvider for camera temp files -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- uCrop Activity for image cropping -->
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.UCrop" />

        <!-- 高德地图 API Key -->
        <meta-data
            android:name="com.amap.api.v2.apikey"
            android:value="${AMAP_API_KEY}" />

        <!-- 数据同步前台服务 -->
        <service
            android:name=".service.sync.DataSyncForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- APK 下载前台服务 -->
        <service
            android:name=".service.update.ApkDownloadService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
    </application>

</manifest>