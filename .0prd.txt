
当前跑步详情页，指标部分(RunDetailHeaderDataCard)的卡片视图，与其余部分视图不一致，且底部与下一张卡的间距也与其他卡的间距不一致，间距偏大；
期望是：
1.间距与其他卡保持一致；
2.卡的效果，与其他卡的效果保持一致；


UserProfileScreen中 当在头像更新后，返回到上一页ProfileTab时，UserProfileCard里的没用同步刷新，修改昵称也如此；

首页的最近一次跑步的卡片，期望展示运动的轨迹，嵌入到卡片内部，应该如何设计


导入fit文件计算当条跑步记录的pb时，期望是每一条


完善HomeTab首页最大数据卡(AllPBAbilityCard)以及pb记录卡(AllPBSpeedCard)的逻辑，真实数据存储在pb_record(PBRecordDao)表里；


周、年、月、总下，心率区间、配速区间的bar里不需要展示具体的数值；


是否纳入统计，逻辑完善，定义参考ios的如下类定义：
enum RecordInClusiveLevel:Int{
    case level_0 = 0 // 不统计不分析
    case level_1 = 1 // 统计& 分析
    case level_2 = 2 // 仅纳入跑量统计，不纳入跑步分析

    // 每总类型对应的颜色
    var color:Color{
        switch self {
        case .level_0:
            return .gray
        case .level_1:
            return .green
        case .level_2:
            return .blue.opacity(0.6)
        }
    }

    var desc:String{
        switch self {
        case .level_0:
           return "不纳入跑量统计，也不纳入跑力分析和PB计算。"
        case .level_1:
            return "纳入跑量统计、跑力分析及PB计算。"
        case .level_2:
            return  "仅纳入跑量统计，不纳入跑力分析及PB计算。"
        }
    }
}

存:

改：

在数据源界面，完善数据源拖动排序功能

期望是在数据源详情界面，执行手动同步时，退出界面，同步逻辑仍然能够在后台执行(android Service形式)，当前已有同步的service,考虑如何做整合；
同时在首页的右上角，也可以看到同步的标识，点击进去可以看到同步的进度；
期望是：
1.数据的拉取，都归一到一个地方，都以service作为承载；
2.单个数据源的详情页，触发手动同步时，退出界面，数据同步仍然能够在后台执行，在首页右上角，也能够看到数据同步的进展；
3.数据同步的状态，全局共享，如果已经在进行同步，不要重复触发同步逻辑；



获取跑步数据列表，可能包含已经解析好的基础数据信息，也可能没有；


期望完善心率区间设置功能；
心率区间设置功能参考下ios的SettingHearRateZonePage；

基于ios的界面，同时增加自动根据服务端获取心率数据并更新的相关数据的开关，开关默认打开，同时旁边增加一个问号，用户点击问号时，向用户解释下这个开关的作用；
同时当有多个平台都获取到了心率数据，增加以哪个平台心率数据获取为准的功能；

当前心率数据获取以及使用相关代码：BaseDataSyncService的buildUserConfig

当设置完心率区间后，计算跑步解析fit时，优先使用设置的值；

当没有设置心率区间，计算跑步数据相关的指标时，默认取最近一次获取到的数据作为计算的依据；

请理解需求、了解当前的代码逻辑，设计需求实现方案；


期望在跑步详情页，二级菜单里；
1.增加修正距离的功能，同时修正距离与原始距离差值不超过10%;
2.增加点击距离以及距离后面的单位，小点点时，可以修改是否纳入统计，目前有三挡，仅统计，不分析 对应inclusiveLevel =2，不统计不分析，对应inclusiveLevel  = 0；
统计又分析，inclusiveLevel = 1;
3.当修改了如上的跑步信息后，需要标记为本地数据有修改，待同步到服务器，下一步会做把新的跑步记录同步到服务器；

请设计实现方案

1.统一下，二级菜单,item前，都不要加图标；

2.修改inclusive level的弹窗，需要展示对应的颜色标识，以及详细的描述，参考代码：

case .level_0:
   return "不纳入跑量统计，也不纳入跑力分析和PB计算。"
case .level_1:
    return "纳入跑量统计、跑力分析及PB计算。"
case .level_2:
    return  "仅纳入跑量统计，不纳入跑力分析及PB计算。"

同时需要抽取为公共组件；

2.在RunRecordItem中，点击设备信息以及右侧的小点点时，也要触发更新inclusiveLevel的逻辑，复用上面的组件；






enum RecordInClusiveLevel:Int{
    case level_0 = 0 // 不统计不分析
    case level_1 = 1 // 统计& 分析
    case level_2 = 2 // 仅纳入跑量统计，不纳入跑步分析

    // 每总类型对应的颜色
    var color:Color{
        switch self {
        case .level_0:
            return .gray
        case .level_1:
            return .green
        case .level_2:
            return .blue.opacity(0.6)
        }
    }

    var desc:String{
        switch self {
        case .level_0:
           return "不纳入跑量统计，也不纳入跑力分析和PB计算。"
        case .level_1:
            return "纳入跑量统计、跑力分析及PB计算。"
        case .level_2:
            return  "仅纳入跑量统计，不纳入跑力分析及PB计算。"
        }
    }

}
